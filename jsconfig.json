/**
 thư mục này để cấu hình đường dẫn trong vscode, ví dụ khi ctrl + click vào mapOder trong server.js thi sẽ nhảy tới file export mapOrder
 */
{
  "compilerOptions": {
    "paths": {
      "~/*": ["./src/*"],
    },
  }
}
